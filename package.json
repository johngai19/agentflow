{
  "name": "agent-frameworks-starter",
  "version": "1.0.0",
  "private": true,
  "description": "Top 20 Agent Frameworks Starter Collection - TDD examples for popular AI agent frameworks",
  "workspaces": [
    "web",
    "packages/typescript/*"
  ],
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "test": "pnpm run test:ts && echo 'Run: pnpm run test:python for Python tests'",
    "test:ts": "pnpm --filter './packages/typescript/**' test",
    "test:web": "pnpm --filter web test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
